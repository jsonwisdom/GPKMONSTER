# GPKMONSTER — Thread Reverse Replay — 2026-09-03

## Root

```text
PROJECT_ROOT      = GPKMONSTER
REPO_OWNER        = jsonwisdom
PROJECT_OPERATOR  = Jason Wisdom / Jay Wisdom
CONTROL_PLANE     = GitHub
EXTERNAL_AUTHORITY_CREATED = FALSE
```

GPKMONSTER is the organizing root for this thread. External platforms are evidence-producing dependencies, not the authority over the project.

## Reverse replay

1. GPKMONSTER factory branch was located at `feat/image-rendering-service-factory`.
2. Five-package build branch `build/gpt-monster-five-zip-v1` was verified at SHA `26f1fb250b3d8704b1e43631bd75247df23e3e2c` before subsequent audit commits.
3. Five planned packages: Zora catalog, Zora media, receipts, KAREN11, PowerShell handoff.
4. Real payload ZIPs were not yet claimed built because Jason's local Drive mirror was required.
5. Quad x Quad Onion audit was added: 16 primary proof edges across source, execution, artifact, receipt.
6. Garbage Pail Journalism Reverse Replay was added: observable failure -> receipt -> published rule/manual -> replay -> mismatch -> burden holder -> verdict.
7. Shopify/Google/Apple authentication was investigated as an external-boundary incident. Screenshots showed passkey/provider confusion and an authentication bootstrap deadlock.
8. Later screenshots showed Jason inside Shopify mobile, `My Store 2`, and a working authenticator-app path.
9. Live Shopify connector replay then passed: store `My Store 2`, plan `Basic`, catalog read successful, product count `0`.
10. Therefore Shopify is not the project root and is not the present control-plane blocker. It is a downstream commerce rail that can be used or ignored.

## Current state

```text
GPKMONSTER_BRANCH           = build/gpt-monster-five-zip-v1
GPKMONSTER_CONTROL          = ACTIVE
GITHUB_REPO_TRUTH           = PRIMARY PROJECT RECORD
QUAD_QUAD_ONION             = INSTALLED
GARBAGE_PAIL_JOURNALISM     = INSTALLED
SHOPIFY_CONNECTOR_READ      = PASS
SHOPIFY_PRODUCTS            = 0
SHOPIFY_REQUIRED_FOR_TRUTH  = FALSE
REAL_FIVE_ZIP_BUILD         = STILL REQUIRES LOCAL PAYLOAD REPLAY
MERGE                       = FALSE
MINT                        = FALSE
PUBLISH                     = FALSE
AUTHORITY_CREATED           = FALSE
```

## Governing boundary

```text
JASON PROJECT DECISION
-> GITHUB SOURCE / RECEIPTS
-> LOCAL REPLAY
-> ARTIFACT
-> RECEIPT

EXTERNAL PLATFORM
-> MAY SUPPLY INPUT / OUTPUT / FAILURE RECEIPT
-> MAY NOT REWRITE GPKMONSTER PROJECT TRUTH
```

## Memory anchor

When this thread drifts into a vendor support loop, return to:

```text
GPKMONSTER > external storefront
GitHub receipt > support narrative
local replay > UI implication
verified edge > inherited assumption
```
